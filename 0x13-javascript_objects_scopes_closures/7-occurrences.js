#!/usr/bin/node
exports.nbOccurences = function (list, searchElement) {
  const count = 0;
  const num = 0;
  if (list[num] === searchElement) count++;
  else { exports.nbOccurences = function (list[num++])}
};
